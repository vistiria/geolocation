development:
  clients:
    default:
      database: geolocation_development
      hosts:
        - localhost:27017
      options:
        server_selection_timeout: 1
test:
  clients:
    default:
      database: geolocation_test
      hosts:
        - localhost:27017
      options:
        server_selection_timeout: 1
